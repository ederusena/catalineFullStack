// array
let items: Array<number> = [1, 2, 3];

// enum
enum Color {
  Red = "#f00a24",
  Green = "#0dff00",
  Blue = "#0d00ff"
}

let red: Color = Color.Red;

// tuple
let tuplas: [string, number];

tuplas = ["hello", 10];
